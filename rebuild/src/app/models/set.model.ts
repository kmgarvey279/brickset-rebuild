export class Set {
  public addImages: number = 0;
  constructor(public name: string, public number: number, public type: string, public themeGroup: string, public theme: string, public subTheme: string, public yearReleased: number, public tags: string, public pieces: number, public retailPrice: number, public pricePerPiece: number, public currentValue: number, public ageRange: number, public packaging: string, public availability: string, public instructions: boolean) {}
}
